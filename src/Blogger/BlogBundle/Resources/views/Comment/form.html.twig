<form 
    action="{{ path('BloggerBlogBundle_comment_create', {'blog_id' : comment.blog.id}) }}"
    method="POST"
    {{ form_enctype(form) }}
    class="blogger"
>
    <p>
        {{ form_widget(form) }}
        <input type="submit" value="Submit">
    </p>
</form>
